# PicSign 实现计划

## 项目概述

为 JPG 照片底部添加白色信息条（摄影者 + 作品名），面向中老年摄影爱好者，操作极简。

- **仓库**：https://github.com/Ayanami-Shinji/PicSign
- **目标用户**：不熟悉复杂软件的中老年用户
- **核心约束**：JPG only、保留 EXIF、批量处理支持单独改标题、Windows 必须能用（macOS 加分）

---

## 技术决策（Cursor 可自主调整）

### JPG "无损+增量" 的现实方案

**事实**：标准 JPEG 格式不支持在底部追加像素而不重编码已有数据。JPEG 的 lossless 操作仅限于旋转、翻转、裁剪（且仅在 MCU 边界）。

**可行方案**：使用最高质量重编码，使得照片区域像素级几乎无差异。

| 工具 | 质量参数 | 色度采样 | 效果 |
|------|---------|---------|------|
| sharp (libvips) | quality=100 | 4:4:4 (no subsample) | 照片区域与原始肉眼无法区分 |
| mozjpeg | quality=95+ | 4:4:4 | 同质量下文件更小 |

**推荐默认配置**：
- `quality: 100`，`chromaSubsampling: '4:4:4'`
- 关闭所有优化选项（trellis、overshoot 等可能引入微变化的开关）
- 文件体积会增大（白条区域的像素数据），但照片区域保真度最高

**可选：智能质量匹配**（进阶功能，非 MVP）：
- 提供一个 quality 滑块（默认 100），用户可手动降低来减小体积
- 或自动二分搜索 quality 使输出体积接近原体积

### EXIF 保留

- sharp 默认丢弃 EXIF，需要显式调用 `.withMetadata()` 或 `.keepExif()`
- 必须保留的字段：相机型号、焦距、光圈、快门、ISO、GPS、拍摄时间、色彩空间
- 可能需要更新 ImageWidth/ImageHeight 元数据以反映白条后的新尺寸

### 技术栈建议

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **Electron + sharp** | 原生文件对话框、跨平台打包成熟、sharp 性能极佳 | 包体 ~150MB | ⭐⭐⭐⭐⭐ |
| **Tauri + sharp** | 包体小 (~10MB)、性能好 | 生态较新、打包配置复杂 | ⭐⭐⭐⭐ |
| **Python + PyQt/PySide + Pillow** | 开发快 | JPG quality 控制不如 sharp 精细、打包麻烦 | ⭐⭐⭐ |
| **Rust + egui/GTK + image-rs** | 极致性能、极小包体 | 开发周期长、UI 不够精致 | ⭐⭐⭐ |

**Cursor 自行评估选择**，只要能满足：
1. Windows 10/11 双击运行（.exe 安装包或绿色包）
2. macOS 加分但非必需
3. JPG quality 精确可控
4. EXIF 保留

---

## 功能详设

### 1. 加载图片

- **方式**：点击按钮选择文件 + 拖拽到窗口
- **格式**：JPG、JPEG、PNG（PNG 导入后统一按 JPG 处理导出）
- **批量**：可一次选择/拖入多张，按文件名排序

### 2. 白色底栏

- 宽度 = 照片宽度
- 高度 = 可配置，默认 100px，范围 50-200px
- 颜色 = 纯白 (#FFFFFF)
- 位置 = 照片底部拼接

### 3. 文字内容

**摄影者**（中间偏左）：
- 格式：`摄影：XXX`
- 首次使用时弹出设置，之后记住
- 批量处理时所有照片共用

**作品名**（右侧）：
- 格式：`《XXX》`
- 自动提取原文件名（不含扩展名）
- 批量处理时每张独立提取，**可单独修改**
- 例如 `白鹭.jpg` → `《白鹭》`
- 支持手动编辑覆盖自动提取

### 4. 文字样式

- **字体**：黑体（默认）、宋体、楷体、微软雅黑
- **字号**：自适应白条高度（白条高度 × 0.6），可手动覆盖
- **颜色**：黑色 #000000
- **水平偏移**：微调左右位置

### 5. 实时预览

- 中央大区域显示合成效果
- 支持缩放适配窗口
- 用户点击文字可直接编辑（或通过侧边面板）

### 6. 导出

- **格式**：JPG only
- **文件名**：`原文件名_签名.jpg`，绝不覆盖原文件
- **保存路径**：默认原始文件所在目录，或弹出保存对话框
- **批量导出**：一键保存所有已编辑照片
- **EXIF**：完整保留原始 EXIF

### 7. 记忆功能

持久化以下设置（用户偏好，非项目配置）：
- 摄影者名字
- 底栏高度
- 字体、字号
- 上次打开目录
- 窗口大小/位置

### 8. 批量处理交互

```
+------------------------------------------+
|  [打开照片]  [全部保存]  [设置]           |
+------------------------------------------+
| 文件列表      |     预览区域              |
| □ 白鹭.jpg   |                            |
| □ 翠鸟.jpg   |    [原图 + 白条预览]       |
| □ 麻雀.jpg   |                            |
|              |                            |
+------------------------------------------+
| 当前编辑：翠鸟.jpg                        |
| 摄影者：[老张____]  作品名：[翠鸟____]    |
| 底栏高度：[100px ▼] 字体：[黑体 ▼]       |
+------------------------------------------+
```

- 左侧文件列表：点击切换预览，复选框控制是否导出
- 中间：当前选中照片的预览
- 底部/右侧：编辑面板，修改的是当前选中照片的设置
- "全部保存"：导出所有勾选的照片

---

## 实现阶段

### Phase 1：核心引擎（CLI 可验证）

- [ ] JPG 读取 + EXIF 提取
- [ ] 白色底栏拼接（canvas resize + fill white）
- [ ] 文字渲染（中文支持）
- [ ] JPG 导出（quality=100, chroma 4:4:4, withMetadata）
- [ ] 验证：原图 vs 导出图照片区域像素差异在 0-1 级别
- [ ] 验证：EXIF 字段完整性
- [ ] 验证：输出文件可用系统图片查看器正常打开

### Phase 2：GUI 骨架

- [ ] 窗口框架 + 菜单/按钮
- [ ] 拖拽区域 + 文件选择对话框
- [ ] 单图预览（原图 + 白条）
- [ ] 编辑面板（摄影者、作品名、底栏高度、字体、字号）
- [ ] 实时预览刷新
- [ ] 单图保存

### Phase 3：批量处理

- [ ] 文件列表组件（缩略图 + 文件名 + 勾选框）
- [ ] 列表切换联动预览和编辑面板
- [ ] 批量保存（进度条）
- [ ] 单独修改每张作品名

### Phase 4：设置与记忆

- [ ] 设置持久化（localStorage / JSON 文件 / SQLite）
- [ ] 启动时恢复上次状态
- [ ] 摄影者名字首次设置引导

### Phase 5：打包发布

- [ ] Windows：.exe 安装包或绿色免安装包
- [ ] macOS：.app / .dmg（如有条件）
- [ ] README：使用说明 + 自行打包步骤
- [ ] 验收测试：拖入 15MB JPG，导出体积可接受，EXIF 完整

---

## 验收标准

1. 拖入一张 15MB 原始 JPG，添加白条和文字后导出：
   - 照片区域肉眼无法分辨与原图差异
   - EXIF（相机型号、焦距、光圈、快门、ISO、GPS、拍摄时间）全部保留
   - 输出文件可被 Windows 照片查看器正常打开
   - 文件体积合理（白条区域增量不可避免，但非白条区域不应有额外膨胀）
2. 重启工具，摄影者名字自动恢复
3. 加载 `翠鸟.jpg`，作品名自动显示 `翠鸟`
4. 批量拖入 5 张不同名字的照片，全部正确导出，文件名正确（`XXX_签名.jpg`），不覆盖原文件
5. 在 Windows 上双击运行（无需安装额外运行时）

---

## 交付物

```
PicSign/
├── README.md           # 使用说明 + 打包步骤
├── PRD.md              # 原始需求文档
├── PLAN.md             # 本文件
├── src/                # 源代码
├── assets/             # 图标等静态资源
├── scripts/            # 打包脚本
│   ├── build-windows.sh
│   └── build-macos.sh
└── .gitignore
```

---

## 给 Cursor 的提示

1. **JPG 质量是核心**：默认 quality=100，chromaSubsampling='4:4:4'，别加优化开关。宁可文件大一点，绝不能损坏照片像素。
2. **EXIF 一个都不能少**：用 withMetadata() 或 keepExif()，验证 GPS 也在。
3. **中文渲染**：确保中文字体在各种系统上都能正常渲染（Windows 字体路径、macOS 字体路径不同）。
4. **UI 极简**：两个主按钮（打开/保存），别搞花里胡哨。父亲级别的易用性。
5. **批量 ≠ 统一**：每张照片的作品名独立可编辑，摄影者名字全局统一。
6. **自选技术栈**：以上建议是 Electron + sharp，但你觉得有更好的尽管用，满足验收标准就行。
