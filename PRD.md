# PicSign（照片签名工具）产品需求文档

**版本**：1.0
**目标用户**：不熟悉复杂软件的中老年摄影爱好者（摄影师父亲）
**核心价值**：为已有的 JPG 照片底部添加白色信息条，操作极简，且尽力保持原始画质与文件体积。

## 1. 项目背景

我父亲是鸟类摄影爱好者。我会用专业软件处理 RAW 格式照片并导出为高质量 JPG（通常 10MB-50MB）。父亲需要一个极其简单的工具，为这些 JPG 照片添加一个干净的白色底栏，打上"摄影：XXX"和"《作品名》"信息。他完全不能接受复杂的操作界面。

## 2. 功能需求

### 2.1 加载图片
- 支持点击"打开照片"按钮或直接拖拽 JPG 文件到窗口。
- 支持的格式：JPG、JPEG、PNG（必须覆盖）。

### 2.2 自动生成白色底栏并填写文字
- 照片底部自动生成一个纯白色矩形区域，宽度等于照片宽度，高度可配置（默认 100px，用户可在设置中调整）。
- 白条上自动生成两处文字：
  - **中间偏左位置**：`摄影：XXX`（例如"摄影：老张"）
  - **右侧位置**：`《作品名》`
- **关键自动化**：
  - 首次使用时可设置默认的"摄影者名字"，之后永久保存，每次打开工具自动填入。
  - 首次加载图片时，"作品名"自动提取**原始文件名（不含扩展名）**，例如加载 `白鹭.jpg`，右侧自动填入 `白鹭`。

### 2.3 实时预览
- 窗口中央大区域显示合成后的效果：原图在上，白色底栏无缝衔接在下。
- 支持缩放或适应窗口，保证完整预览。
- 用户可直接在预览图上点击文字区域修改（或通过右侧面板修改）。

### 2.4 编辑与微调
- 侧边栏或底部栏提供简洁的控制项（可隐藏/折叠）：
  - 摄影者：文本输入框
  - 作品名：文本输入框
  - 字体：一个下拉菜单，提供常用中文字体（黑体、宋体、楷体、微软雅黑等）
  - 字号：滑块或数字输入（默认自适应白条高度，也可手动调整）
  - 文字颜色：固定为黑色（或可选项，默认黑）
  - 底栏高度：滑块（50px - 200px）
  - 文字水平偏移：微调左右位置（高级选项）

### 2.5 导出照片
- 点击"保存照片"按钮，生成带底栏的新文件。
- 保存路径：默认在原始文件所在文件夹，或弹出保存对话框。
- 文件名：原始文件名 + `_签名` 后缀，例如 `白鹭_签名.jpg`。务必不要覆盖原始文件。
- 支持批量处理：一次加载多张照片，自动沿用同一个"摄影者"名字和底栏设置，批量导出。文件名自动取各自的原始文件名。

### 2.6 记忆功能
- 记住以下设置，下次启动时自动恢复：
  - 摄影者名字
  - 底栏高度
  - 字体及字号偏好
  - 上次使用的文件夹路径

## 3. 非功能需求

### 3.1 画质与EXIF
- 照片区域尽可能无损
- EXIF（相机型号、焦距、光圈、快门、ISO、GPS、拍摄时间）完整保留
- 导出格式：JPG only

### 3.2 易用性
- 界面干净，两个主按钮
- 文字编辑无需二级菜单
- 启动时有醒目拖拽提示

### 3.3 跨平台
- 必须支持 Windows 10/11，macOS 加分

### 3.4 离线与隐私
- 完全离线运行，无需网络
- 所有图片处理均在本地完成

## 4. 用户界面草图

```
+---------------------------------------------+
|  PicSign - 照片签名工具                     |
+---------------------------------------------+
|  [打开照片]  [保存照片]  [设置（齿轮图标）]   |
+---------------------------------------------+
|                                             |
|     (照片预览区域，原图+底部白条)            |
|                                             |
|                                             |
+---------------------------------------------+
| 白色底栏区域：                              |
|         摄影：老张          《白鹭》         |
+---------------------------------------------+
| 编辑面板（可折叠）：                        |
| 摄影者：[老张____]  作品名：[白鹭____]      |
| 底栏高度：[100px ▼] 字号：[36px ▼]         |
| 字体：[黑体 ▼]                             |
+---------------------------------------------+
```

## 5. 技术选型

由 Cursor 自主评估选择最合适的技术栈，满足：
1. JPG quality 精确可控，支持 4:4:4 色度采样
2. EXIF 完整保留
3. 傻瓜式 GUI
4. 跨平台（至少 Windows）
5. 打包为双击即可运行的 .exe

## 6. 交付物

- 所有源代码
- 依赖说明或环境配置文件
- Windows 打包脚本/步骤
- README 使用说明

## 7. 验收标准

1. 拖入 15MB JPG，添加白条和文字后导出，照片区域肉眼无损，EXIF 完整
2. 重启工具，摄影者名字自动出现
3. 加载 `翠鸟.jpg`，作品名自动显示"翠鸟"
4. 批量拖入 5 张不同名字的照片，全部正确导出，文件名正确
5. Windows 双击即可运行
